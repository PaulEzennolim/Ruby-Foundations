# Assign values to variables x and y
x = "An arbitrary example"
y = "Some other example"

# Check if the length of x is greater than the length of y
if x.length > y.length then
  # If true, print a message indicating that x is longer than y
  puts "x is longer than y"
else
  # If false, print a message indicating that x is shorter than y
  puts "x is shorter than y"
end
